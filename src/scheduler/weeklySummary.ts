export async function weeklySummary() {
  console.log("[📘 weeklySummary] Generating weekly summary doc...");

  // Later: call minds-eye-gworkspace-connectors/docWriter.ts
  // and produce Docs summaries.

  return { ok: true, message: "Weekly summary generated" };
}
